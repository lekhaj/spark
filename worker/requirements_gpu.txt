# ── GPU Worker Requirements ───────────────────────────────────────────────────
# Install with:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   pip install xformers --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_gpu.txt

# ── Deep Learning ─────────────────────────────────────────────────────────────
# torch + torchvision installed via --index-url (see install_gpu.sh)
diffusers==0.27.2
transformers==4.40.2
accelerate==0.30.1
safetensors==0.4.3
omegaconf
controlnet-aux==0.0.7

# ── 3D Generation (Hunyuan3D-2.1) ─────────────────────────────────────────────
trimesh==4.3.2
einops
scikit-image
# hy3dgen installed from github: pip install git+https://github.com/tencent/Hunyuan3D-2.git

# ── Background Removal ────────────────────────────────────────────────────────
rembg[gpu]==2.0.56

# ── AWS / Infrastructure ──────────────────────────────────────────────────────
boto3==1.34.100
botocore>=1.34.0
redis==5.0.4
pymongo==4.7.2
python-dotenv==1.0.1

# ── Image Processing ──────────────────────────────────────────────────────────
Pillow==10.3.0
opencv-python-headless==4.9.0.80

# ── Utilities ─────────────────────────────────────────────────────────────────
requests
tqdm
psutil
