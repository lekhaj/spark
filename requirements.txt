fastapi
uvicorn
redis
pymongo
boto3
gradio
requests
python-dotenv
pydantic-settings

# The following Blender modules are only available inside Blender's Python:
# bpy, bmesh, mathutils
# Do NOT install them via pip; they are bundled with Blender.

# Optional/experimental:
# torch           # Uncomment if using PyTorch
# hunyuan3d       # Uncomment if using hunyuan3d
