# Local workspace infra details (SSH paths, IPs — not for version control)
LOCAL_INFRA.md

# Claude Code session metadata (worktrees, locks, transcripts)
.claude/

# Environment variables and secrets
env/
.env
.env.secrets
.env.bak.*
.env.cpu.bak.*
.env.gpu.bak.*

!.env.example
!.env.cpu
!.env.gpu
.aws
# Python cache files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
!deploy/lib/
!deploy/lib/**
# worker/lib/ holds tracked Python modules; only un-ignore .py to avoid
# letting __pycache__ / *.so back in via the lib/ exception.
!worker/lib/
!worker/lib/*.py
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv/
.python-version

# Testing
.pytest_cache/
.coverage
htmlcov/
coverage.xml
nosetests.xml

# Output directories
output/
images/
generated_images/

# VSCode
.vscode/

# PyCharm
.idea/

# Jupyter Notebook
.ipynb_checkpoints

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# Logs
logs/
*.log
/gradio_cache

config.py
/outputs
# Local Claude session worktrees
.claire/

# Vendored upstream / local-only artifacts (GPU box clones Hunyuan3D from
# GitHub at bootstrap — see deploy/install/hunyuan3d.sh; never track it here)
Hunyuan3D-2.1/
3D_pipeline_compress/
generated_files/
.gradio/
