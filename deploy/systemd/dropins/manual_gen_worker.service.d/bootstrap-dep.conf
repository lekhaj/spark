# Drop-in: gate the GPU worker behind spark-bootstrap.
# bootstrap pulls latest git + installs any missing models / pip deps before
# the worker starts. Failed bootstrap → worker stays down → status panel shows
# the failure rather than the worker crash-looping on missing imports.

[Unit]
Requires=spark-bootstrap.service
After=spark-bootstrap.service
